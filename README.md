```
#Passos executados para subida para o GIT
    - git init = iniciou a conexão com o git
    - git add . = adicionou todos os arquivos alterados e criados
    - git commit -m 'nome do commit' = pré-subida para o git com os arquivos comentados
    - git push origin <nome da branch> = subida para o git apontando a branch que vai receber os arquivos  

```
```
#Comando git
    - git branch = vizualiza todas as branchs criadas
    - 
```
